services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    volumes:
      - redis_data:/data

  backend:
    build:
      context: .
    restart: unless-stopped
    environment:
      NODE_ENV: staging
      PORT: 5006
      SERVER_URL: ${SERVER_URL}
      DB_URL: mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongo:27017/zoomx_digital_staging?authSource=admin
      CORS_ORIGIN: ${CORS_ORIGIN}
      BCRYPT_SALT_ROUNDS: ${BCRYPT_SALT_ROUNDS:-10}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET}
      JWT_ACCESS_EXPIRES_IN: ${JWT_ACCESS_EXPIRES_IN:-1d}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      JWT_REFRESH_EXPIRES_IN: ${JWT_REFRESH_EXPIRES_IN:-7d}
      JWT_OTP_SECRET: ${JWT_OTP_SECRET}
      JWT_PASS_RESET_SECRET: ${JWT_PASS_RESET_SECRET}
      JWT_PASS_RESET_EXPIRES_IN: ${JWT_PASS_RESET_EXPIRES_IN:-1h}
      SENDER_EMAIL: ${SENDER_EMAIL}
      SENDER_APP_PASS: ${SENDER_APP_PASS}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_NAME: ${ADMIN_NAME}
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      R2_BUCKET: ${R2_BUCKET}
      R2_PUBLIC_URL: ${R2_PUBLIC_URL}
      FRONTEND_URL: ${FRONTEND_URL}
      REVALIDATE_SECRET: ${REVALIDATE_SECRET}
    depends_on:
      - mongo
      - redis
    ports:
      - "5006:5006"

volumes:
  mongo_data:
  redis_data:
